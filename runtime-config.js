// Optional deployment override. GitHub Pages uses the Render default in app.js;
// a self-hosted deployment can set this value before app.js is loaded.
window.GRIDSCOPE_API_BASE_URL = window.GRIDSCOPE_API_BASE_URL || '';
